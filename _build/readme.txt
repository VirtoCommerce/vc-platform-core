Script for building and making nuget packages for virtocommerce platform 

Run build.cmd with optional path to artifact folder
If run without parameters artifact folder will be Platform in current directory

Run make-packages.cmd with optional path to artifact folder
If run without parameters artifact folder will be packages\Platform in current directory